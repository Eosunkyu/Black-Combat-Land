# 소프트 삭제 기능 구현 가이드

## 1. 데이터베이스 마이그레이션 실행

먼저 데이터베이스에 소프트 삭제를 위한 컬럼들을 추가해야 합니다.

```bash
# MySQL에 접속하여 다음 SQL 파일 실행
mysql -u your_username -p your_database < db_soft_delete_migration.sql
```

또는 MySQL 클라이언트에서 직접 실행:

```sql
-- posts 테이블에 소프트 삭제 관련 컬럼 추가
ALTER TABLE posts ADD COLUMN is_deleted TINYINT(1) NOT NULL DEFAULT 0;
ALTER TABLE posts ADD COLUMN deleted_at DATETIME NULL;
ALTER TABLE posts ADD COLUMN deleted_by INT NULL;

-- comments 테이블에 소프트 삭제 관련 컬럼 추가
ALTER TABLE comments ADD COLUMN is_deleted TINYINT(1) NOT NULL DEFAULT 0;
ALTER TABLE comments ADD COLUMN deleted_at DATETIME NULL;
ALTER TABLE comments ADD COLUMN deleted_by INT NULL;

-- 외래키 제약조건 추가
ALTER TABLE posts ADD FOREIGN KEY (deleted_by) REFERENCES users(id) ON DELETE SET NULL;
ALTER TABLE comments ADD FOREIGN KEY (deleted_by) REFERENCES users(id) ON DELETE SET NULL;

-- 인덱스 추가 (성능 향상)
CREATE INDEX idx_posts_is_deleted ON posts(is_deleted);
CREATE INDEX idx_comments_is_deleted ON comments(is_deleted);
CREATE INDEX idx_posts_deleted_at ON posts(deleted_at);
CREATE INDEX idx_comments_deleted_at ON comments(deleted_at);
```

## 2. 구현된 기능

### 2.1 사용자 관점에서의 변화

- **게시글/댓글 삭제**: 기존과 동일하게 "삭제" 버튼을 누르면 삭제됩니다
- **화면에서 사라짐**: 삭제된 게시글/댓글은 더 이상 일반 사용자에게 보이지 않습니다
- **사용자 경험**: 기존과 동일한 사용자 경험을 제공합니다

### 2.2 관리자 관점에서의 새로운 기능

#### 게시글 관리 페이지 (/admin/posts)
- **삭제된 게시물 보기**: `show_deleted=1` 파라미터로 삭제된 게시물 확인 가능
- **일괄 처리 옵션**:
  - `soft_delete`: 소프트 삭제 (복구 가능)
  - `restore`: 삭제된 게시물 복구
  - `permanent_delete`: 완전 삭제 (복구 불가능)

#### 삭제된 게시글 관리 페이지 (/admin/deleted_posts)
- 삭제된 모든 게시글을 한 곳에서 관리
- 누가 언제 삭제했는지 추적 정보 표시
- 개별 복구 또는 완전 삭제 가능

### 2.3 데이터베이스 변화

#### 새로 추가된 컬럼:
- `is_deleted`: 삭제 여부 (0: 활성, 1: 삭제됨)
- `deleted_at`: 삭제된 시각
- `deleted_by`: 삭제한 사용자 ID (익명의 경우 NULL)

#### 기존 데이터 보존:
- 기존의 모든 게시글과 댓글 데이터가 그대로 유지됩니다
- 삭제된 게시글도 데이터베이스에서 완전히 제거되지 않습니다

## 3. 이점

### 3.1 데이터 보존
- **실수 복구**: 잘못 삭제한 게시글을 복구할 수 있습니다
- **기록 유지**: 모든 게시글 기록이 데이터베이스에 보존됩니다
- **통계 정확성**: 전체 게시글 수 등의 통계가 정확하게 유지됩니다

### 3.2 관리 편의성
- **감사 추적**: 누가 언제 무엇을 삭제했는지 추적 가능
- **유연한 관리**: 삭제된 게시물을 선별적으로 복구하거나 완전 삭제 가능
- **법적 요구사항**: 데이터 보존 의무가 있는 경우 유용

### 3.3 성능
- **빠른 삭제**: 실제 DELETE 작업 대신 UPDATE로 더 빠른 처리
- **인덱스 최적화**: is_deleted 컬럼에 인덱스를 추가하여 조회 성능 향상

## 4. 주의사항

### 4.1 기존 기능과의 호환성
- 모든 게시글 조회 쿼리에 `is_deleted = 0` 조건이 자동으로 추가됩니다
- 댓글 개수, 좋아요 개수도 삭제되지 않은 것만 계산됩니다

### 4.2 디스크 공간
- 삭제된 데이터가 계속 누적되므로 주기적인 완전 삭제가 필요할 수 있습니다
- 관리자가 필요에 따라 오래된 삭제 데이터를 완전 삭제할 수 있습니다

## 5. 사용법

### 5.1 일반 사용자
기존과 동일하게 사용하면 됩니다. 삭제 기능에 변화가 없습니다.

### 5.2 관리자
1. **삭제된 게시물 확인**: `/admin/deleted_posts` 페이지에서 확인
2. **게시물 복구**: 삭제된 게시물 목록에서 "복구" 버튼 클릭
3. **완전 삭제**: 삭제된 게시물 목록에서 "완전 삭제" 버튼 클릭
4. **일괄 처리**: 게시물 관리 페이지에서 여러 게시물을 선택하여 일괄 처리

## 6. 기술적 세부사항

### 6.1 수정된 파일
- `routes/board.py`: 게시물/댓글 삭제 로직을 소프트 삭제로 변경
- `routes/admin.py`: 관리자 기능에 소프트 삭제/복구 기능 추가
- `db_soft_delete_migration.sql`: 데이터베이스 스키마 변경

### 6.2 주요 변경사항
- 모든 게시물 조회 쿼리에 `WHERE is_deleted = 0` 조건 추가
- 삭제 로직을 `DELETE` 대신 `UPDATE SET is_deleted = 1` 사용
- 관리자용 삭제된 게시물 관리 인터페이스 추가

이제 게시물 삭제 시 데이터가 완전히 사라지지 않고 기록용으로 보관되며, 필요시 복구할 수 있습니다!
